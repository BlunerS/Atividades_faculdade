def pular_nuvens(nuvens):
    saltos = 0
    indice = 0
    while indice < (len(nuvens) - 2):
        if nuvens[indice + 2] != 1:
            # 1
            # 2
        else:
            # 3
            # 4
    return saltos

print(pular_nuvens(nuvens))